# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build poker-svc`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t poker-svc`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system poker-svc && \
          adduser --system -G poker-svc poker-svc

COPY dist/apps/poker-svc poker-svc/
RUN chown -R poker-svc:poker-svc .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix poker-svc --omit=dev -f install

CMD [ "node", "poker-svc" ]
